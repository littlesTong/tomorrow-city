{
  "name": "tomorrow-city",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "balance": "node tools/balance.mjs",
    "serve": "python3 -m http.server 4317 --directory dist",
    "ios:sync": "npx cap sync ios",
    "ios:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capgo/native-purchases": "^8.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
